server:
  port: 2008
spring:
  datasource:
    password: root
    username: root
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql:///spring_demo
  application:
    name: microserver-provider-user
logging:
  level:
    root: info
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10086/eureka
  instance:
    prefer-ip-address: true